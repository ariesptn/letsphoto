<!DOCTYPE html>
<html>

<head></head>

<body>
    <div>
        <form action="/reply/<%=req.params.id%>" method="POST">
            <input type="text" name="comment">
            <input type="submit" value="Reply">
        </form>
    </div>
    <% commentReplyData.forEach(commentReply=>{ %>
    <div>
        <a href="/user/<%= commentReply.User.username %>">
            <%= commentReply.User.username %></a> :
        <%= commentReply.comment %>
        <br>
        <%= commentReply.createdAt.toISOString().split("T").join(' ').split('.')[0] %>
    </div>
    <%})%>
</body>

</html>